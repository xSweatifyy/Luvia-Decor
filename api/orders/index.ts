import { neon } from '@neondatabase/serverless';
import { Resend } from 'resend';
import type { VercelRequest, VercelResponse } from '@vercel/node';
const sql=neon(process.env.DATABASE_URL||'');
const STATUSES=['nova','zpracovava_se','dokonceno','zruseno'] as const;type Status=typeof STATUSES[number];
const labels:Record<Status,string>={nova:'Nová objednávka',zpracovava_se:'Objednávka se zpracovává',dokonceno:'Objednávka dokončena',zruseno:'Objednávka zrušena'};
const esc=(v:unknown)=>String(v??'').replace(/[&<>\'\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]||c));
async function ensureTable(){await sql`CREATE TABLE IF NOT EXISTS orders (id TEXT PRIMARY KEY, data JSONB NOT NULL, created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`;}
function orderId(req:VercelRequest){const q=req.query?.orderId||req.query?.id;if(typeof q==='string'&&q)return decodeURIComponent(q);if(Array.isArray(q)&&q[0])return decodeURIComponent(String(q[0]));const m=String(req.url||'').match(/\/api\/orders\/([^/?]+)\/status/);return m?decodeURIComponent(m[1]):'';}
async function sendStatusEmail(order:any,status:Status){const key=process.env.RESEND_API_KEY?.trim(),to=String(order?.customer?.email||'').trim();if(!key)throw new Error('Na Vercelu chybí RESEND_API_KEY. Ověřená doména nestačí, aplikace potřebuje Resend API klíč.');if(!to)throw new Error('Objednávka nemá e-mail zákazníka.');const label=labels[status],number=String(order.orderNumber||order.id||''),first=status==='nova';const intro=first?'děkujeme Vám za Váš zájem o naše produkty a za vytvoření objednávky. Vaší objednávky si vážíme a nyní ji pečlivě zpracujeme. Brzy Vás budeme kontaktovat s informacemi o dalším postupu.':`rádi bychom Vás informovali, že stav Vaší objednávky <strong>${esc(number)}</strong> byl aktualizován.`;const html=`<!doctype html><html lang="cs"><body style="margin:0;background:#f5f1ec;color:#2d2723;font-family:Arial,sans-serif"><div style="padding:32px 12px"><div style="max-width:640px;margin:auto;background:#fff;border:1px solid #e8dfd5;box-shadow:0 10px 32px rgba(45,39,35,.08)"><div style="padding:34px 28px;text-align:center;background:#2d2723;color:#faf6f0"><div style="font-family:Georgia,serif;font-size:29px;letter-spacing:4px;font-weight:700">LUVIA DECOR</div><div style="margin-top:9px;color:#d8c9b7;font-size:11px;letter-spacing:1.8px;text-transform:uppercase">Květinový ateliér &amp; dekorace</div></div><div style="height:4px;background:#a48763"></div><div style="padding:34px 30px"><div style="font-size:11px;text-transform:uppercase;letter-spacing:1.7px;color:#a48763;font-weight:700">Aktualizace objednávky</div><h1 style="font-family:Georgia,serif;font-size:27px;margin:9px 0 8px">${esc(label)}</h1><div style="font-size:13px;color:#817469">Objednávka ${esc(number)}</div><hr style="border:0;border-top:1px solid #eee7df;margin:25px 0"><p style="font-size:15px;line-height:1.8">Dobrý den, <strong>${esc(order.customer?.fullName||'')}</strong>,</p><p style="font-size:15px;line-height:1.8">${intro}</p><div style="margin:26px 0;padding:22px 20px;background:#f8f5f1;border:1px solid #ebe3da"><div style="font-size:10px;color:#8c7355;text-transform:uppercase;letter-spacing:1.5px;font-weight:700">Aktuální stav objednávky</div><div style="font-family:Georgia,serif;font-size:22px;font-weight:600;margin-top:7px">${esc(label)}</div></div><p style="font-size:15px;line-height:1.8">${first?'Jakmile budeme mít další informace k Vaší objednávce, ozveme se Vám.':'O další změně stavu Vás budeme informovat e-mailem.'}</p><p style="font-size:15px;line-height:1.8">Děkujeme za Vaši důvěru a přejeme Vám krásný den.</p><p style="font-family:Georgia,serif;font-size:16px;line-height:1.7">S přáním hezkého dne,<br><strong>Tým Luvia Decor</strong></p></div><div style="padding:22px 30px;background:#f5efe6;text-align:center;color:#817469;font-size:12px;line-height:1.8">Luvia Decor &bull; U Rejdiště 3732/15, 767 01 Kroměříž<br><a href="mailto:podpora@luvia-decor.cz" style="color:#8c7355">podpora@luvia-decor.cz</a></div></div></div></body></html>`;const r=await new Resend(key).emails.send({from:'Luvia Decor <no-reply@luvia-decor.cz>',to:[to],subject:`${label} – ${number}`,html});if(r.error)throw new Error(`Resend: ${r.error.message}`);return r.data?.id||null;}
async function updateStatus(req:VercelRequest,res:VercelResponse,id:string){if(!id)return res.status(400).json({error:'Chybí id objednávky.'});let rows=await sql`SELECT id,data FROM orders WHERE id=${id} LIMIT 1`;if(!rows.length)rows=await sql`SELECT id,data FROM orders WHERE data->>'orderNumber'=${id} LIMIT 1`;if(!rows.length)return res.status(404).json({error:`Objednávka ${id} nebyla nalezena.`});const status=String(req.body?.status||'').trim() as Status;if(!STATUSES.includes(status))return res.status(400).json({error:'Neplatný stav objednávky.'});const old=(rows[0].data||{}) as any,oldStatus=String(old.status||''),storedId=String(rows[0].id),order={...old,id:storedId,status};await sql`UPDATE orders SET data=${JSON.stringify(order)}::jsonb,updated_at=NOW() WHERE id=${storedId}`;if(oldStatus===status)return res.status(200).json({success:true,order,statusEmailSent:false,message:'Stav objednávky se nezměnil, e-mail nebyl odeslán.'});try{const emailId=await sendStatusEmail(order,status);return res.status(200).json({success:true,order,statusEmailSent:true,statusEmailId:emailId});}catch(e:any){const errorMessage=e?.message||'E-mail se nepodařilo odeslat.';console.error('Order status email failed:',e);return res.status(200).json({success:true,order,statusEmailSent:false,statusEmailError:errorMessage,message:`Stav objednávky byl uložen, ale e-mail se nepodařilo odeslat: ${errorMessage}`});}}
export default async function handler(req:VercelRequest,res:VercelResponse){res.setHeader('Cache-Control','no-store');res.setHeader('Pragma','no-cache');res.setHeader('Expires','0');if(!process.env.DATABASE_URL)return res.status(500).json({error:'DATABASE_URL není nastavená ve Vercelu.'});try{await ensureTable();if(req.method==='GET'){const rows=await sql`SELECT data FROM orders ORDER BY created_at DESC`;return res.status(200).json(rows.map(r=>r.data));}if(req.method==='POST'){if(req.body?.action==='update_status'||req.body?.action==='updateStatus')return updateStatus(req,res,String(req.body.orderId||req.body.id||''));const {customer,items,customNote}=req.body||{};if(!customer?.fullName||!customer?.email||!customer?.phone)return res.status(400).json({error:'Chybí povinné kontaktní údaje.'});if(!Array.isArray(items)||!items.length)return res.status(400).json({error:'Košík je prázdný.'});const orderItems=items.map((i:any)=>({productId:i.productId||i.id||i.sku||'custom',title:i.title||'Produkt',price:Number(i.price)||0,quantity:Number(i.quantity)||1,imageUrl:i.imageUrl||'',customNote:i.customNote||''}));const subtotal=orderItems.reduce((s:number,i:any)=>s+i.price*i.quantity,0);const order:any={id:`ord-${Date.now()}`,orderNumber:`LUV-${new Date().getFullYear()}-${Math.floor(1000+Math.random()*9000)}`,createdAt:new Date().toISOString(),customer:{fullName:customer.fullName,email:customer.email,phone:customer.phone,street:customer.street||'',city:customer.city||'',zip:customer.zip||'',country:customer.country||'Česká republika',note:customer.note||customNote||''},items:orderItems,subtotal,shipping:0,totalPrice:subtotal,status:'nova',resendSent:false};await sql`INSERT INTO orders(id,data) VALUES(${order.id},${JSON.stringify(order)}::jsonb)`;return res.status(201).json({success:true,order});}if(['PUT','PATCH'].includes(req.method||''))return updateStatus(req,res,orderId(req));return res.status(405).json({error:'Metoda není podporovaná.'});}catch(e:any){console.error('Orders API:',e);return res.status(500).json({error:e?.message||'Objednávku se nepodařilo zpracovat.'});}}
