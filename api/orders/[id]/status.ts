import type { VercelRequest, VercelResponse } from '@vercel/node';
import { neon } from '@neondatabase/serverless';
import { Resend } from 'resend';

const sql = neon(process.env.DATABASE_URL || '');
type OrderStatus = 'nova' | 'zpracovava_se' | 'zaplaceno' | 'u_prepravce' | 'odeslano' | 'dokonceno' | 'zruseno';
const STATUS_LABELS: Record<OrderStatus, string> = { nova:'Nová objednávka', zpracovava_se:'Objednávka se zpracovává', zaplaceno:'Objednávka zaplacena', u_prepravce:'Objednávka je u přepravce', odeslano:'Objednávka odeslána', dokonceno:'Objednávka dokončena', zruseno:'Objednávka zrušena' };
const STATUS_MESSAGES: Record<OrderStatus, string> = { nova:'Vaše objednávka byla přijata.', zpracovava_se:'Vaši objednávku jsme začali zpracovávat.', zaplaceno:'Platbu za Vaši objednávku jsme zaznamenali.', u_prepravce:'Vaše objednávka byla předána přepravci.', odeslano:'Vaše objednávka byla odeslána.', dokonceno:'Vaše objednávka byla dokončena. Děkujeme za Vaši důvěru.', zruseno:'Vaše objednávka byla zrušena. Pokud potřebujete více informací, kontaktujte nás.' };
const FOOTER = `<div style="padding:24px 32px;background:#faf8f5;border-top:1px solid #eee5dc;text-align:center;color:#756b63;font-size:12px;line-height:1.8"><strong style="color:#302923">Luvia Decor</strong><br>Odpovědná osoba: Ladislav Pekárek<br>Adresa: U Rejdiště 3732/15, 767 01, Kroměříž<br>IČO: 29905061<br>Email: objednavky@luvia-decor.cz · podpora@luvia-decor.cz<br>Telefon: +420702345999</div>`;
function escapeHtml(value: unknown): string { return String(value ?? '').replace(/[&<>\"']/g, char => ({ '&':'&amp;', '<':'&lt;', '>':'&gt;', '\"':'&quot;', "'":'&#39;' }[char] || char)); }
function variableSymbol(orderNumber: string): string { return String(orderNumber || '').replace(/\D/g, ''); }
function carrierLabel(order: any): string { return String(order?.delivery?.carrier || order?.deliveryCarrier || 'neuvedený přepravce'); }
function money(value: unknown): string { return Number(value || 0).toLocaleString('cs-CZ'); }
function paymentQrUrl(order: any): string { const iban='CZ45550000000000963625011'; const amount=Number(order.totalPrice||0).toFixed(2); const vs=String(order.variableSymbol||variableSymbol(order.orderNumber)).replace(/\D/g,''); const note=String(order.orderNumber||'').replace(/[^a-zA-Z0-9 ._-]/g,'').slice(0,60); const payload=`SPD*1.0*ACC:${iban}*AM:${amount}*CC:CZK*X-VS:${vs}*X-MSG:${note}`; return `https://api.qrserver.com/v1/create-qr-code/?size=220x220&margin=8&data=${encodeURIComponent(payload)}`; }
function emailLayout(title:string,content:string):string { return `<!doctype html><html lang="cs"><body style="margin:0;background:#f4f0eb;font-family:Arial,Helvetica,sans-serif;color:#302923"><div style="padding:32px 12px"><div style="max-width:680px;margin:0 auto;background:#fff;border:1px solid #e7dfd6;border-radius:20px;overflow:hidden;box-shadow:0 8px 30px rgba(45,39,35,.08)"><div style="background:#211c18;padding:28px 32px;text-align:center"><div style="font-size:26px;letter-spacing:5px;font-weight:700;color:#faf6f0">LUVIA DECOR</div><div style="margin-top:7px;font-size:11px;letter-spacing:2px;text-transform:uppercase;color:#c5a880">Květinový ateliér &amp; dekorace</div></div><div style="padding:32px"><div style="font-size:12px;letter-spacing:2px;text-transform:uppercase;color:#b08f65;font-weight:700;margin-bottom:8px">${escapeHtml(title)}</div>${content}</div>${FOOTER}</div></div></body></html>`; }
function itemsHtml(order:any):string { const items=Array.isArray(order.items)?order.items:[]; if(!items.length)return '<tr><td colspan="3" style="padding:12px;color:#777">Žádné položky</td></tr>'; return items.map((item:any)=>`<tr><td style="padding:12px;border-bottom:1px solid #eee"><strong>${escapeHtml(item.title)}</strong><br><span style="font-size:11px;color:#8b8178">ID produktu: ${escapeHtml(item.productId||item.id||'neuvedeno')}</span></td><td style="padding:12px;border-bottom:1px solid #eee;text-align:center">${Number(item.quantity||1)}</td><td style="padding:12px;border-bottom:1px solid #eee;text-align:right;white-space:nowrap">${money(item.price)} Kč</td></tr>`).join(''); }
async function sendStatusEmails(order:any,status:OrderStatus){ const apiKey=process.env.RESEND_API_KEY; const customerEmail=String(order?.customer?.email||'').trim(); const sellerEmail=String(process.env.ORDER_NOTIFY_EMAIL||'objednavky@luvia-decor.cz').trim(); if(!apiKey)throw new Error('Chybí RESEND_API_KEY ve Vercel Environment Variables.'); if(!customerEmail)throw new Error('Objednávka nemá e-mail zákazníka.'); const resend=new Resend(apiKey); const label=STATUS_LABELS[status]; const customerName=escapeHtml(order?.customer?.fullName||'zákazníku'); const orderNumber=escapeHtml(order?.orderNumber||order?.id); const vs=escapeHtml(order?.variableSymbol||variableSymbol(order?.orderNumber)); const carrier=escapeHtml(carrierLabel(order)); const items=itemsHtml(order); const payment=(status==='nova'||status==='zpracovava_se')?`<div style="margin-top:24px;padding:20px;background:#faf8f5;border:1px solid #eee5dc;border-radius:14px"><div style="font-weight:700;margin-bottom:10px">Bankovní převod</div><div style="font-size:13px;line-height:1.8">Číslo účtu: <strong>963625011/5500</strong><br>IBAN: <strong>CZ45 5500 0000 0096 3625 011</strong><br>Variabilní symbol: <strong>${vs}</strong><br>Částka: <strong>${money(order.totalPrice)} Kč</strong><br>Poznámka pro příjemce: <strong>${orderNumber}</strong></div><div style="text-align:center;margin-top:16px"><img src="${paymentQrUrl(order)}" width="220" height="220" alt="QR platba" style="display:inline-block;border:1px solid #e6ded5;border-radius:8px"></div></div>`:''; const customerContent=`<h1 style="margin:0 0 12px;font-size:25px">${escapeHtml(label)}</h1><p style="font-size:15px;line-height:1.7">Dobrý den, ${customerName},<br>${escapeHtml(STATUS_MESSAGES[status])}</p><div style="margin:22px 0;padding:16px 18px;background:#faf8f5;border-radius:12px;font-size:13px;line-height:1.8"><strong>Objednávka:</strong> ${orderNumber}<br><strong>Variabilní symbol:</strong> ${vs}<br><strong>Přepravce:</strong> ${carrier}</div><table style="width:100%;border-collapse:collapse;font-size:13px"><thead><tr><th style="padding:10px;text-align:left;border-bottom:1px solid #ddd">Položka</th><th style="padding:10px">Ks</th><th style="padding:10px;text-align:right">Cena</th></tr></thead><tbody>${items}</tbody></table><p style="font-size:17px;text-align:right;margin:20px 0 0"><strong>Celkem: ${money(order.totalPrice)} Kč</strong></p>${payment}<p style="margin-top:24px;font-size:13px;color:#756b63">V případě dotazů nám odpovězte na tento e-mail nebo nás kontaktujte na objednavky@luvia-decor.cz.</p>`; const sellerContent=`<h1 style="margin:0 0 12px;font-size:25px">Nová objednávka</h1><div style="margin:20px 0;padding:16px 18px;background:#faf8f5;border-radius:12px;font-size:13px;line-height:1.8"><strong>Objednávka:</strong> ${orderNumber}<br><strong>Zákazník:</strong> ${escapeHtml(order?.customer?.fullName)}<br><strong>E-mail:</strong> ${escapeHtml(customerEmail)}<br><strong>Telefon:</strong> ${escapeHtml(order?.customer?.phone)}<br><strong>Přepravce:</strong> ${carrier}</div><table style="width:100%;border-collapse:collapse;font-size:13px"><thead><tr><th style="padding:10px;text-align:left;border-bottom:1px solid #ddd">Produkt</th><th style="padding:10px">Ks</th><th style="padding:10px;text-align:right">Cena</th></tr></thead><tbody>${items}</tbody></table><p style="font-size:17px;text-align:right;margin:20px 0 0"><strong>Celkem: ${money(order.totalPrice)} Kč</strong></p>`; const sends:Promise<any>[]=[resend.emails.send({from:'Luvia Decor <objednavky@luvia-decor.cz>',to:customerEmail,replyTo:'podpora@luvia-decor.cz',subject:`${label} · ${order.orderNumber} | Luvia Decor`,html:emailLayout('Informace k objednávce',customerContent)})]; if(status==='nova')sends.push(resend.emails.send({from:'Luvia Decor <objednavky@luvia-decor.cz>',to:sellerEmail,replyTo:'podpora@luvia-decor.cz',subject:`Nová objednávka · ${order.orderNumber} | Luvia Decor`,html:emailLayout('Nová objednávka',sellerContent)})); const results=await Promise.allSettled(sends); const failed=results.find((result:any)=>result.status==='rejected'||result.value?.error); if(failed){const reason=failed.status==='rejected'?failed.reason?.message:failed.value?.error?.message; throw new Error(reason||'Resend odmítl odeslání e-mailu.');} return {customer:true,seller:status==='nova',sentAt:new Date().toISOString()}; }
export default async function handler(req:VercelRequest,res:VercelResponse){ if(req.method!=='PUT')return res.status(405).json({error:'Metoda není podporovaná.'}); try{ const requestedId=String(req.query.id||'').trim(); const status=String(req.body?.status||'') as OrderStatus; const resendEmail=Boolean(req.body?.resendEmail); if(!requestedId)return res.status(400).json({error:'Chybí ID objednávky.'}); if(!Object.prototype.hasOwnProperty.call(STATUS_LABELS,status))return res.status(400).json({error:'Neplatný stav objednávky.'}); const rows=await sql`SELECT id,data FROM orders WHERE id=${requestedId} OR data->>'orderNumber'=${requestedId} LIMIT 1`; if(!rows.length)return res.status(404).json({error:'Objednávka nebyla nalezena podle ID ani čísla objednávky.'}); const dbId=String(rows[0].id); const order={...(rows[0].data||{}),id:dbId}; const previousStatus=String(order.status||'nova') as OrderStatus; const changed=previousStatus!==status; const updatedOrder={...order,status,updatedAt:new Date().toISOString()}; await sql`UPDATE orders SET data=${JSON.stringify(updatedOrder)}::jsonb WHERE id=${dbId}`; if(changed||resendEmail){ try{ const emailStatus=await sendStatusEmails(updatedOrder,status); updatedOrder.statusEmail={sent:true,status,...emailStatus}; updatedOrder.resendSent=true; delete updatedOrder.resendError; }catch(emailError:any){ console.error('Order status email error:',emailError); updatedOrder.statusEmail={sent:false,status,error:emailError?.message||'Odeslání e-mailu selhalo.',failedAt:new Date().toISOString()}; updatedOrder.resendSent=false; updatedOrder.resendError=emailError?.message||'Odeslání e-mailu selhalo.'; } await sql`UPDATE orders SET data=${JSON.stringify(updatedOrder)}::jsonb WHERE id=${dbId}`; } return res.status(200).json({...updatedOrder,success:true,statusChanged:changed,emailAttempted:changed||resendEmail}); }catch(error:any){ console.error('Order status API error:',error); return res.status(500).json({error:error?.message||'Chyba serveru.'}); } }
