import type { VercelRequest, VercelResponse } from '@vercel/node';
import { neon } from '@neondatabase/serverless';
import { Resend } from 'resend';
const sql = neon(process.env.DATABASE_URL || '');
const ZASLAT_API = 'https://www.zaslat.cz/api/v1';
const ZASLAT_CARRIERS = ['PPL','DPD','GLS','WEDO','BALIKOVNA','ZASILKOVNA'];
const ZASLAT_PACKAGE = { weight: 5, width: 20, height: 20, length: 20 };
const ZASLAT_FUEL_SURCHARGE = 0.105;
const VAT_RATE = 0.21;
const FIXED_SHIPPING: Record<string, { pickup: number; address?: number }> = {
  BALIKOVNA: { pickup: 52, address: 88 }, DPD: { pickup: 85, address: 98 }, GLS: { pickup: 52 }, WEDO: { pickup: 57, address: 92 }, PPL: { pickup: 68 }, ZASILKOVNA: { pickup: 62, address: 89 }
};
const PPL_ALLOWED_CATEGORIES = ['doplňky & ostatní','vánoce','svíčky','umělé květiny & ostatní umělé','věnce','vázy & keramika & květináče'];
const normalizeCategory = (value: unknown) => String(value || '').trim().toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g, '').replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '');
const PPL_ALLOWED = new Set(PPL_ALLOWED_CATEGORIES.map(normalizeCategory));
const isPplAllowed = (items: any[]) => items.length > 0 && items.every((item:any) => PPL_ALLOWED.has(normalizeCategory(item.category)));
const finalShipping = (baseExVat: number) => baseExVat * (1 + ZASLAT_FUEL_SURCHARGE) * (1 + VAT_RATE);
const fixedShipping = (carrier: string, method: string, country: string, items: any[]) => {
  if (country === 'SK') return carrier === 'ZASILKOVNA' ? 0 : null;
  if (carrier === 'PPL' && !isPplAllowed(items)) return null;
  const cfg = FIXED_SHIPPING[carrier]; if (!cfg) return null;
  if (carrier === 'ZASILKOVNA' && method === 'address') { const weight = items.reduce((sum:number, item:any) => sum + Math.max(0, Number(item.weight || 0)) * Math.max(1, Math.floor(Number(item.quantity) || 1)), 0); if (weight > 5) return null; }
  const base = method === 'address' ? cfg.address : cfg.pickup; return base == null ? null : finalShipping(base);
};
const DEFAULT_SHIPPING = { carriers: Object.fromEntries(ZASLAT_CARRIERS.map(c => [c, { enabled: true, address: FIXED_SHIPPING[c]?.address ?? 0, pickup_point: FIXED_SHIPPING[c]?.pickup ?? 0, box: FIXED_SHIPPING[c]?.pickup ?? 0 }])) as any, personalPickup: { enabled: true, price: 0, label: 'Osobní odběr – Kroměříž' } };
async function ensureTables() { await sql`CREATE TABLE IF NOT EXISTS orders (id TEXT PRIMARY KEY, data JSONB NOT NULL, created_at TIMESTAMPTZ NOT NULL DEFAULT NOW())`; await sql`CREATE TABLE IF NOT EXISTS coupons (id TEXT PRIMARY KEY, code TEXT UNIQUE NOT NULL, type TEXT NOT NULL, value NUMERIC NOT NULL, active BOOLEAN NOT NULL DEFAULT TRUE, created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), note TEXT, category_ids JSONB NOT NULL DEFAULT '[]'::jsonb, remaining_value NUMERIC)`; await sql`ALTER TABLE coupons ADD COLUMN IF NOT EXISTS category_ids JSONB NOT NULL DEFAULT '[]'::jsonb`; await sql`ALTER TABLE coupons ADD COLUMN IF NOT EXISTS remaining_value NUMERIC`; await sql`UPDATE coupons SET category_ids = '[]'::jsonb WHERE category_ids IS NULL`; await sql`UPDATE coupons SET remaining_value = value WHERE note = 'gift-voucher' AND remaining_value IS NULL`; }
async function getShippingConfig() { try { await sql`CREATE TABLE IF NOT EXISTS app_state (id INT PRIMARY KEY, data JSONB NOT NULL, updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW())`; const rows = await sql`SELECT data FROM app_state WHERE id = 1 LIMIT 1`; const saved = rows[0]?.data?.shipping; if (!saved) return DEFAULT_SHIPPING; const carriers = Object.fromEntries(Object.entries({ ...DEFAULT_SHIPPING.carriers, ...(saved.carriers || {}) }).map(([name, cfg]: [string, any]) => [name, { enabled: cfg?.enabled !== false, address: Math.max(0, Number(cfg?.address ?? DEFAULT_SHIPPING.carriers[name]?.address ?? 0)), pickup_point: Math.max(0, Number(cfg?.pickup_point ?? cfg?.pickupPoint ?? DEFAULT_SHIPPING.carriers[name]?.pickup_point ?? 0)), box: Math.max(0, Number(cfg?.box ?? cfg?.pickup_point ?? cfg?.pickupPoint ?? DEFAULT_SHIPPING.carriers[name]?.box ?? 0)) }])); return { carriers, personalPickup: { enabled: saved.personalPickup?.enabled !== false, price: Math.max(0, Number(saved.personalPickup?.price ?? 0)), label: String(saved.personalPickup?.label || DEFAULT_SHIPPING.personalPickup.label) } }; } catch { return DEFAULT_SHIPPING; } }
function variableSymbol(orderNumber: string): string { return String(orderNumber || '').replace(/\D/g, ''); }
function escapeHtml(value: unknown): string { return String(value ?? '').replace(/[&<>\"']/g, char => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '\"': '&quot;', "'": '&#39;' }[char] || char)); }
function money(value: unknown): string { return Number(value || 0).toLocaleString('cs-CZ', { minimumFractionDigits: 2, maximumFractionDigits: 2 }); }
function paymentQrUrl(order: any): string { const params = new URLSearchParams({ accountNumber: '963625003', bankCode: '5500', amount: Number(order.totalPrice || 0).toFixed(2), currency: 'CZK', vs: String(order.variableSymbol || variableSymbol(order.orderNumber) || '').replace(/\D/g, ''), message: String(order.orderNumber || '').trim() }); return `https://api.paylibo.com/paylibo/generator/czech/image?${params.toString()}`; }
function emailLayout(title: string, content: string): string { return `<!doctype html><html lang="cs"><body style="margin:0;background:#f4f0eb;font-family:Arial,Helvetica,sans-serif;color:#302923"><div style="padding:32px 12px"><div style="max-width:680px;margin:0 auto;background:#fff;border:1px solid #e7dfd6;border-radius:20px;overflow:hidden;box-shadow:0 8px 30px rgba(45,39,35,.08)"><div style="background:#211c18;padding:28px 32px;text-align:center"><div style="font-size:26px;letter-spacing:5px;font-weight:700;color:#faf6f0">LUVIA DECOR</div><div style="margin-top:7px;font-size:11px;letter-spacing:2px;text-transform:uppercase;color:#c5a880">Květinový ateliér &amp; dekorace</div></div><div style="padding:32px"><div style="font-size:12px;letter-spacing:2px;text-transform:uppercase;color:#b08f65;font-weight:700;margin-bottom:8px">${escapeHtml(title)}</div>${content}</div><div style="padding:24px 32px;background:#faf8f5;border-top:1px solid #eee5dc;text-align:center;color:#756b63;font-size:12px;line-height:1.8"><strong style="color:#302923">Luvia Decor</strong><br>Odpovědná osoba: Ladislav Pekárek<br>Adresa: U Rejdiště 3732/15, 767 01, Kroměříž<br>IČO: 29905061<br>Email: objednavky@luvia-decor.cz · podpora@luvia-decor.cz<br>Telefonní číslo: +420702345999</div></div></div></body></html>`; }
function itemsHtml(items: any[], shipping?: { carrier?: string; price?: number }): string { const productRows = items.map((item: any) => `<tr><td style="padding:12px;border-bottom:1px solid #eee"><strong>${escapeHtml(item.title)}</strong><br><span style="font-size:11px;color:#8b8178">ID produktu: ${escapeHtml(item.productId || item.id || 'neuvedeno')}</span></td><td style="padding:12px;border-bottom:1px solid #eee;text-align:center">${Number(item.quantity || 1)}</td><td style="padding:12px;border-bottom:1px solid #eee;text-align:right;white-space:nowrap">${money(item.price)} Kč</td></tr>`).join(''); const carrierName = String(shipping?.carrier || '').trim() || 'Osobní odběr – Kroměříž'; const shippingRow = shipping ? `<tr><td style="padding:12px;border-bottom:1px solid #eee"><strong>Doprava (${escapeHtml(carrierName)})</strong></td><td style="padding:12px;border-bottom:1px solid #eee;text-align:center">1×</td><td style="padding:12px;border-bottom:1px solid #eee;text-align:right;white-space:nowrap">${money(shipping.price)} Kč</td></tr>` : ''; return productRows + shippingRow || '<tr><td colspan="3" style="padding:12px;color:#777">Žádné položky</td></tr>'; }
async function sendOrderEmails(order: any): Promise<any> { const apiKey = process.env.RESEND_API_KEY; const customerEmail = String(order?.customer?.email || '').trim(); const sellerEmail = String(process.env.ORDER_NOTIFY_EMAIL || 'objednavky@luvia-decor.cz').trim(); if (!apiKey) throw new Error('Chybí RESEND_API_KEY ve Vercelu Environment Variables.'); if (!customerEmail) throw new Error('Objednávka nemá e-mail zákazníka.'); const resend = new Resend(apiKey); const customer = order.customer || {}; const items = Array.isArray(order.items) ? order.items : []; const delivery = order.delivery || {}; const pickupAddress = String(delivery.pickupPointAddress || delivery.pickupAddress || (delivery.pickupPoint && typeof delivery.pickupPoint === 'object' ? (delivery.pickupPoint.formattedAddress || delivery.pickupPoint.address || [delivery.pickupPoint.street, delivery.pickupPoint.zip, delivery.pickupPoint.city].filter(Boolean).join(', ')) : '') || '').trim(); const pickupLabel = String(delivery.pickupPoint || '').trim(); const pickupDetails = [pickupLabel, pickupAddress].filter(Boolean).join(' · '); const deliveryText = delivery.method === 'personal_pickup' ? 'Osobní odběr – Kroměříž' : delivery.method === 'box' ? `Box – ${delivery.carrier || ''}: ${pickupDetails}` : delivery.method === 'pickup_point' ? `Výdejní místo – ${delivery.carrier || ''}: ${pickupDetails}` : `Doručení na adresu – ${delivery.carrier || ''}`; const paymentBlock = `<div style="margin-top:24px;padding:20px;background:#faf8f5;border:1px solid #eee5dc;border-radius:14px"><div style="font-weight:700;margin-bottom:10px">Bankovní převod</div><div style="font-size:13px;line-height:1.8">Číslo účtu (ČR): <strong>963625003/5500</strong><br>IBAN: <strong>CZ96 5500 0000 0009 6362 5003</strong><br>SWIFT/BIC: <strong>RZBCCZPP</strong><br>Variabilní symbol: <strong>${escapeHtml(order.variableSymbol || variableSymbol(order.orderNumber))}</strong><br>Částka: <strong>${money(order.totalPrice)} Kč</strong><br>Poznámka pro příjemce: <strong>${escapeHtml(order.orderNumber)}</strong></div><div style="text-align:center;margin-top:16px"><img src="${paymentQrUrl(order)}" width="220" height="220" alt="QR platba" style="display:inline-block;border:1px solid #e6ded5;border-radius:8px"></div></div>`; const common = `<div style="margin:18px 0;padding:16px 18px;background:#faf8f5;border-radius:12px;font-size:13px;line-height:1.8"><strong>Objednávka:</strong> ${escapeHtml(order.orderNumber)}<br><strong>Variabilní symbol:</strong> ${escapeHtml(order.variableSymbol || variableSymbol(order.orderNumber))}<br><strong>Doručení:</strong> ${escapeHtml(deliveryText)}</div><table style="width:100%;border-collapse:collapse;font-size:13px"><thead><tr><th style="padding:10px;text-align:left;border-bottom:1px solid #ddd">Položka</th><th style="padding:10px">Ks</th><th style="padding:10px;text-align:right">Cena</th></tr></thead><tbody>${itemsHtml(items, { carrier: delivery.carrier, price: Number(order.shipping || 0) })}</tbody></table><p style="font-size:17px;text-align:right;margin:20px 0 0"><strong>Celkem: ${money(order.totalPrice)} Kč</strong></p>${paymentBlock}`; const customerHtml = emailLayout('Potvrzení objednávky', `<h1 style="margin:0 0 12px;font-size:25px">Děkujeme za Vaši objednávku</h1><p style="font-size:15px;line-height:1.7">Dobrý den, ${escapeHtml(customer.fullName)},<br>Vaše objednávka byla úspěšně přijata. Jakmile obdržíme platbu, budeme ji dále zpracovávat.</p>${common}<p style="margin-top:24px;font-size:13px;color:#756b63">O změně stavu objednávky Vás budeme informovat e-mailem.</p>`); const sellerHtml = emailLayout('Nová objednávka', `<h1 style="margin:0 0 12px;font-size:25px">Nová objednávka ${escapeHtml(order.orderNumber)}</h1><p style="font-size:14px;line-height:1.7"><strong>Zákazník:</strong> ${escapeHtml(customer.fullName)}<br><strong>E-mail:</strong> ${escapeHtml(customer.email)}<br><strong>Telefon:</strong> ${escapeHtml(customer.phone)}<br><strong>Adresa:</strong> ${escapeHtml(customer.street)}, ${escapeHtml(customer.zip)} ${escapeHtml(customer.city)}<br><strong>Poznámka:</strong> ${escapeHtml(customer.note || '—')}</p>${common}`); const results = await Promise.allSettled([resend.emails.send({ from: 'Luvia Decor <objednavky@luvia-decor.cz>', to: customerEmail, replyTo: 'podpora@luvia-decor.cz', subject: `Potvrzení objednávky · ${order.orderNumber} | Luvia Decor`, html: customerHtml }), resend.emails.send({ from: 'Luvia Decor <objednavky@luvia-decor.cz>', to: sellerEmail, subject: `NOVÁ OBJEDNÁVKA · ${order.orderNumber} | Luvia Decor`, html: sellerHtml })]); const failed = results.find((r: any) => r.status === 'rejected' || r.value?.error); if (failed) throw new Error(failed.status === 'rejected' ? failed.reason?.message : failed.value?.error?.message); return { customer: true, seller: true, sentAt: new Date().toISOString() }; }

async function zaslatRates(fromCountry: string, toCountry: string, packages: any[], deliveryBranch?: string) { const apiKey = process.env.ZASLAT_API_KEY; if (!apiKey) throw new Error('Chybí ZASLAT_API_KEY ve Vercelu.'); const body: any = { currency: 'CZK', pickup_date: new Date().toISOString().slice(0,10), from: { country: fromCountry }, to: { country: toCountry }, packages }; if (deliveryBranch) body.delivery_branch = deliveryBranch; const r = await fetch(`${ZASLAT_API}/rates/get`, { method:'POST', headers:{'Accept':'application/json','Content-Type':'application/json','X-Apikey':apiKey}, body:JSON.stringify(body) }); const d = await r.json().catch(()=>null); if (!r.ok || d?.status >= 400) throw new Error(d?.message || 'Zaslat API nevrátilo nabídku dopravy.'); return Array.isArray(d?.rates) ? d.rates : []; }

async function handler(req: VercelRequest, res: VercelResponse) {
  if (req.method === 'OPTIONS') return res.status(204).end();
  try {
    if (req.method === 'POST' && String(req.query.action || '') === 'rates') { const { fromCountry='CZ', toCountry='CZ', packages=[ZASLAT_PACKAGE], deliveryBranch } = req.body || {}; const rates = await zaslatRates(String(fromCountry), String(toCountry), Array.isArray(packages) && packages.length ? packages : [ZASLAT_PACKAGE], deliveryBranch ? String(deliveryBranch) : undefined); return res.status(200).json({ rates, availableCarriers: [...new Set(rates.map((r:any) => String(r.carrier || '').trim()).filter(Boolean))], package: ZASLAT_PACKAGE }); }
    await ensureTables();
    if (req.method === 'GET') { const rows = await sql`SELECT id, data FROM orders ORDER BY created_at DESC`; return res.status(200).json(rows.map((row: any) => ({ ...(row.data || {}), id: row.id }))); }
    if (req.method !== 'POST') return res.status(405).json({ error: 'Metoda není podporovaná.' });
    const { customer, items, couponCode, delivery, paymentMethod } = req.body || {};
    if (!customer?.fullName || !customer?.email || !customer?.phone) return res.status(400).json({ error: 'Chybí povinné kontaktní údaje.' });
    if (!Array.isArray(items) || items.length === 0) return res.status(400).json({ error: 'Košík je prázdný.' });
    if (paymentMethod && paymentMethod !== 'bank_transfer') return res.status(400).json({ error: 'Jediný dostupný způsob platby je bankovní převod.' });
    const shippingConfig = await getShippingConfig(); const deliveryMethod = String(delivery?.method || 'address'); const carrier = String(delivery?.carrier || '').toUpperCase();
    if (deliveryMethod === 'personal_pickup') { if (!shippingConfig.personalPickup.enabled) return res.status(400).json({ error: 'Osobní odběr momentálně není dostupný.' }); }
    else { const carrierConfig = shippingConfig.carriers[carrier] || null; if (!carrierConfig || carrierConfig.enabled === false) return res.status(400).json({ error: 'Zvolený dopravce není dostupný.' }); if (!['address','pickup_point','box'].includes(deliveryMethod)) return res.status(400).json({ error: 'Neplatný způsob doručení.' }); if ((deliveryMethod === 'pickup_point' || deliveryMethod === 'box') && !String(delivery?.pickupPoint || '').trim()) return res.status(400).json({ error: 'Je nutné vybrat výdejní místo nebo box.' }); if (deliveryMethod === 'address' && (!String(customer.street || '').trim() || !String(customer.city || '').trim() || !String(customer.zip || '').trim())) return res.status(400).json({ error: 'Pro doručení na adresu je nutné vyplnit celou adresu.' }); }
    let subtotal = 0;
    const orderItems = items.map((item: any) => { const price = Math.max(0, Number(item.price) || 0); const quantity = Math.max(1, Math.floor(Number(item.quantity) || 1)); subtotal += price * quantity; return { productId: item.productId || item.id || 'custom', title: String(item.title || ''), price, quantity, imageUrl: item.imageUrl || '', customNote: item.customNote || '', category: String(item.category || ''), weight: Number(item.weight || 0) }; });
    let shipping = deliveryMethod === 'personal_pickup' ? shippingConfig.personalPickup.price : 0;
    if (deliveryMethod !== 'personal_pickup') { const country = String(customer.country || '').toLowerCase().includes('slov') ? 'SK' : 'CZ'; const fixed = fixedShipping(carrier, deliveryMethod, country, orderItems); if (fixed == null) return res.status(400).json({ error: carrier === 'PPL' ? 'PPL je dostupné pouze pro vybrané kategorie.' : carrier === 'ZASILKOVNA' && deliveryMethod === 'address' ? 'Zásilkovna na adresu je dostupná pouze do 5 kg.' : 'Zvolený způsob dopravy není pro tuto objednávku dostupný.' }); shipping = fixed; }
    let discount = 0; let appliedCouponCode: string | undefined; let voucherConsumed = 0; let voucherId: string | undefined;
    if (couponCode) { const code = String(couponCode).trim().toUpperCase(); const rows = await sql`SELECT id, code, type, value, active, note, category_ids, remaining_value FROM coupons WHERE code = ${code} AND active = TRUE LIMIT 1`; if (!rows.length) return res.status(400).json({ error: 'Slevový kód je neplatný nebo deaktivovaný.' }); const coupon = rows[0]; const categoryIds = Array.isArray(coupon.category_ids) ? coupon.category_ids.map(String) : []; const allowed = new Set(categoryIds.map(normalizeCategory)); const eligibleItems = allowed.size ? orderItems.filter((item: any) => allowed.has(normalizeCategory(item.category))) : orderItems; if (allowed.size && !eligibleItems.length) return res.status(400).json({ error: 'Tento slevový kód nelze použít na žádný produkt v košíku.' }); const eligibleSubtotal = eligibleItems.reduce((sum: number, item: any) => sum + item.price * item.quantity, 0); appliedCouponCode = coupon.code; if (coupon.note === 'gift-voucher') { const remaining = Number(coupon.remaining_value ?? coupon.value); if (remaining <= 0) return res.status(400).json({ error: 'Dárkový poukaz je již vyčerpán.' }); discount = Math.min(remaining, eligibleSubtotal); voucherConsumed = discount; voucherId = coupon.id; } else if (coupon.type === 'percent') discount = Math.min(eligibleSubtotal, Math.round(eligibleSubtotal * Number(coupon.value) / 100)); else discount = Math.min(eligibleSubtotal, Number(coupon.value) || 0); }
    if (voucherId && voucherConsumed > 0) { const updated = await sql`UPDATE coupons SET remaining_value = GREATEST(0, COALESCE(remaining_value, value) - ${voucherConsumed}), active = CASE WHEN COALESCE(remaining_value, value) - ${voucherConsumed} <= 0 THEN FALSE ELSE active END WHERE id = ${voucherId} AND active = TRUE AND COALESCE(remaining_value, value) >= ${voucherConsumed} RETURNING remaining_value`; if (!updated.length) return res.status(409).json({ error: 'Dárkový poukaz byl mezitím použit. Zkontrolujte jeho zbývající hodnotu.' }); }
    const id = `ord-${Date.now()}-${Math.random().toString(36).slice(2, 7)}`; const orderNumber = `LUV-${new Date().getFullYear()}-${Math.floor(1000 + Math.random() * 9000)}`; const vs = variableSymbol(orderNumber);
    const normalizedDelivery = deliveryMethod === 'personal_pickup' ? { method: 'personal_pickup' } : { method: deliveryMethod, carrier, ...(deliveryMethod === 'pickup_point' || deliveryMethod === 'box' ? { pickupPoint: String(delivery?.pickupPoint || '').trim() } : {}), ...(delivery?.zaslatPickupBranch ? { zaslatPickupBranch: String(delivery.zaslatPickupBranch) } : {}), shippingBaseExVat: deliveryMethod === 'address' ? FIXED_SHIPPING[carrier]?.address : FIXED_SHIPPING[carrier]?.pickup, fuelSurchargeRate: 10.5, shippingIncludesVat: true };
    const newOrder: any = { id, orderNumber, variableSymbol: vs, paymentMethod: 'bank_transfer', createdAt: new Date().toISOString(), customer: { fullName: customer.fullName, email: customer.email, phone: customer.phone, street: customer.street || '', city: customer.city || '', zip: customer.zip || '', country: customer.country || 'Česká republika', note: customer.note || '' }, items: orderItems, subtotal, shipping, discount: discount || undefined, couponCode: appliedCouponCode, giftVoucherUsed: voucherConsumed || undefined, totalPrice: Math.max(0, subtotal - discount + shipping), delivery: normalizedDelivery, status: 'nova', emails: { customer: false, seller: false } };
    try { await sql`INSERT INTO orders (id, data) VALUES (${id}, ${JSON.stringify(newOrder)}::jsonb)`; } catch (insertError) { if (voucherId && voucherConsumed > 0) await sql`UPDATE coupons SET remaining_value = COALESCE(remaining_value, value) + ${voucherConsumed}, active = TRUE WHERE id = ${voucherId}`; throw insertError; }
    try { newOrder.emails = await sendOrderEmails(newOrder); } catch (emailError: any) { newOrder.emails = { customer: false, seller: false, error: emailError?.message || 'Odeslání e-mailu selhalo.', failedAt: new Date().toISOString() }; }
    await sql`UPDATE orders SET data = ${JSON.stringify(newOrder)}::jsonb WHERE id = ${id}`;
    return res.status(201).json({ success: true, order: newOrder });
  } catch (error: any) { console.error('Orders API error:', error); return res.status(500).json({ error: error?.message || 'Chyba serveru.' }); }
}
export default handler;
